# Feature definition

The following table presents the definition of all the stock features provided with `musif`.

The first column are Python Regular Expressions that match the relevant columns of the
final DataFrame.

The `.+` at the beginning of a pattern stands for the scope prefix of the column:
`Part<Abbreviation>_` (one part), `Sound<Abbreviation>_` (all parts of one instrument),
`Family<Abbreviation>_` (all parts of one family), or `Score_` (all parts). Each row
states the scopes at which the feature is actually produced. Staves that resolve to the
same part abbreviation are treated as one logical part (counts summed, extremes merged).
For a sound performed by exactly one part, every `Sound<X>_` column equals the
corresponding `Part<X>_` column by construction (the sound scope aggregates over that
one part); the `DataProcessor` option `delete_duplicated_sound_columns` drops these
duplicates, keeping the per-part means and part counts.

Conventions used throughout:

* every `_Per` column is a proportion between 0 and 1, not a percentage;
* chords (double stops, keyboard chords) are excluded from note-based features, although
  a measure containing only chords still counts as sounding;
* scale degrees in minor keys are measured against the **natural** minor scale (the key
  signature), so a raised leading tone is `Degree#7`;
* in windowed extraction only complete windows are analyzed (a tail shorter than one
  window is skipped) and per-window rows have `Measures` equal to the window size.

Finally, the _Stock module_ column lists the module distributed in `musif` that
generates each feature. The stock feature modules are: `core`, `ambitus`, `melody`,
`tempo`, `density`, `texture`, `harmony`, `lyrics`, `scale`, `scale_relative`, `key`,
`dynamics`, `rhythm` and `music21`, plus the basic modules `scoring` and
`file_name_generic`.

Note that none of these features works on percussion instruments.

In addition to the features in the present page, the features in the `'music21'` stock
module (first row of the below table) provide all the features from [`music21.features.native`](http://web.mit.edu/music21/doc/moduleReference/moduleFeaturesNative.html).

| **Column RegEx**                   | **Explanation**                                                                                                                                                                                                                           | Stock module      |
|------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------|
| `m21_.*`                           | Music21 features from `music21.features.native`. The `QualityFeature` extractor (P22) is excluded: it duplicated `Mode` and read only notated key signatures, contradicting the actual mode. The column set is identical with and without caching | `music21`            |
| `Id`                               | Numeric identifier of the file within the extraction                                                                                                                                                                                      | added by the extractor |
| `WindowId`                         | Index of the window within its score (0 when no windowing is used)                                                                                                                                                                        | added by the extractor |
| `WindowRange`                      | Half-open range of 0-based measure indices covered by the window, e.g. `0 - 8`                                                                                                                                                            | added by the extractor |
| `FileName`                         | File name of the original score                                                                                                                                                                                                           | `core`            |
| `Artist`                           | Text before the first underscore of the file name (empty when there is none)                                                                                                                                                              | `file_name_generic` |
| `Title`                            | Text after the first underscore of the file name (the whole stem when there is none)                                                                                                                                                      | `file_name_generic` |
| `Measures`                         | No. of measures of the piece (of the window, in windowed extraction)                                                                                                                                                                      | `core`            |
| `.+_SoundingMeasures$`             | No. of measures that have at least one note (not all silence). Part scope counts the part's own measures; sound/family/score scopes are sums over their parts                                                                             | `core`            |
| `.+_SoundingMeasuresMean$`         | Sounding measures per part, i.e. the summed sounding measures / no. of parts, at sound/family/score scope                                                                                                                                 | `core`            |
| `.+_Notes$`                        | No. of notes. Part scope counts the part; sound/family/score scopes are sums over their parts                                                                                                                                             | `core`            |
| `.+_NotesMean$`                    | Notes per part, i.e. the summed notes / no. of parts, at sound/family/score scope                                                                                                                                                         | `core`            |
| `TimeSignature`                    | Initial time signature of the piece (later changes are not reported here)                                                                                                                                                                 | `tempo`           |
| `TimeSignatureGrouped`             | Type of meter of the initial time signature: simple duple, simple triple, compound duple, compound triple, compound quadruple (12/X metres), or other                                                                                                                       | `tempo`           |
| `NumberOfBeats`                    | No. of beats **per measure** implied by the initial time signature                                                                                                                                                                        | `tempo`           |
| `Key`                              | Key in which the piece is written. The relation to annotated key signature may not be straightforward                                                                                                                                     | `key`             |
| `KeySignature`                     | Key signature of the piece according to modern convention; might not correspond to the one annotated on the score                                                                                                                         | `key`             |
| `FamilyInstrumentation`            | Comma-separated abbreviations of the instrumental (non-voice) families present on the score                                                                                                                                               | `scoring`         |
| `FamilyScoring`                    | Comma-separated abbreviations of all families present on the score, voices included                                                                                                                                                       | `scoring`         |
| `SoundScoring`                     | Comma-separated sound abbreviations of everything present on the score, voices included                                                                                                                                                   | `scoring`         |
| `Instrumentation`                  | Instruments on the score, using Italian abbreviation; 2 parts of the same instrument are simplified as 1 such instrument                                                                                                                  | `scoring`         |
| `Scoring`                          | List of parts on the score, including voices, using Italian abbreviation; 2 parts of the same instrument are not simplified; various parts of the same instrument/voice are indicated by Roman numerals attached to the part abbreviation | `scoring`         |
| `.+_NumberOfParts$`                | Number of parts found on the score, for each sound and family and for the whole score                                                                                                                                                     | `scoring`         |
| `.+_NumberOfFilteredParts$`        | Number of parts **kept** by `parts_filter` (equal to `NumberOfParts` when no filter is set), for each sound and family and for the whole score                                                                                             | `scoring`         |
| `Voices`                           | Voice parts included on the score (with Roman numerals when a voice type appears more than once), ordered from high to low when the voice type is known                                                                                    | `scoring`         |
| `KeySignatureType`                 | Type of `KeySignature` (`b` flats, `n` naturals, or `s` sharps) according to modern convention                                                                                                                                            | `key`             |
| `Mode`                             | Mode of the `Key` of the piece                                                                                                                                                                                                            | `key`             |
| `NumericTempo`                     | Numeric tempo of the piece, as defined by the engravers and correctors in the MusicXML files                                                                                                                                              | `tempo`           |
| `Tempo`                            | Verbal annotation for the tempo, as defined by the composer (read from the first measure only)                                                                                                                                            | `tempo`           |
| `TempoGrouped1`                    | Classification of `Tempo` according to the main term in the verbal annotation                                                                                                                                                             | `tempo`           |
| `TempoGrouped2`                    | Classification of `Tempo` in Slow, Moderate, or Fast (`NA` when no tempo term is recognized)                                                                                                                                              | `tempo`           |
| `.+_Density$`                      | Notes per beat: summed notes of the scope's parts / summed per-measure beats of those parts (metre changes are respected at every scope, and a single-part sound equals its part). Produced at part/sound/family/score scope               | `density`         |
| `.+_SoundingDensity$`              | Notes per sounding beat: summed notes / summed per-measure beats of the sounding measures, a ratio of sums over the scope's parts. Produced at part/sound/family/score scope                                                               | `density`         |
| `Harmony_Analysis_Available`       | 1 when the harmonic features were successfully extracted from the MuseScore analysis file, 0 otherwise (no analysis file, or the extraction failed)                                                                                       | `harmony`         |
| `Harmony_Chord_.+_(Count\|Per)`    | No. / proportion of harmonic annotations for each chord label, built as the annotation's numeral plus its chord type (`V7`, `viio`, ...; all major/minor seventh qualities collapse to `7`, and `#viio` chords are counted with `viio`)   | `harmony`         |
| `Harmony_Chord_types_.+`           | Proportion of harmonic annotations of each chord type: major triad, minor triad, 7th, dim, aug, aug6, other                                                                                                                               | `harmony`         |
| `Harmony_Chords_Grouping_1.+_(Count\|Per)` | No. / proportion of chords performing the [T, SD, D, etc.] function in the piece, as determined by the Riemannian classification function; it distinguishes between minor and major functions. Chords whose label cannot be classified are excluded from both grouping levels | `harmony`         |
| `Harmony_Chords_Grouping_2.+_(Count\|Per)` | No. / proportion of chords performing the [T, SD, D, etc.] function in the piece, as determined by the Riemannian classification function; it does not distinguish between minor and major. Note: `Grouping_1` `T`/`t`/`MED`/`med` hold a single numeral each (they equal `Numerals_I/i/III/iii`), and `Grouping_2` `NAP`/`bSD`/`#SD` only merge the case pair of the same `Grouping_1` bucket | `harmony`         |
| `Harmony_HarmonicRhythm`           | Harmonic rhythm of the piece, calculated as no. of harmonic annotations / `Measures`                                                                                                                                                      | `harmony`         |
| `Harmony_HarmonicRhythmBeats`      | Harmonic rhythm of the piece by beats, calculated as no. of harmonic annotations / total no. of beats (beats are summed measure by measure, following the time signature in force)                                                        | `harmony`         |
| `Harmony_Key_.+_PercentageMeasures` | Proportion of measures that are in a given local key, extracted from the LocalKey column of the harmonic analysis                                                                                                                        | `harmony`         |
| `Harmony_Key_Modulatory_.+`        | Proportion of key areas (sections) spent in a given local key, extracted from the LocalKey column of the harmonic analysis                                                                                                                | `harmony`         |
| `Harmony_Key_(D\|d\|Dom\|SD\|sd\|SubD\|T\|rel\|Other)_PercentageMeasures` and `Harmony_Key_Modulatory_(D\|d\|Dom\|SD\|sd\|SubD\|T\|rel\|Other)` | Riemannian groupings of the two rows above (D=V, d=v, Dom=D+d, SD=IV/II/VI, sd=iv/ii, SubD=SD+sd, T=I/i, rel=III/vi), added by the `DataProcessor` when `grouped_analysis` is on. `D`, `d`, `T` and their `Modulatory` twins are single-member sums, so they duplicate the `V`/`v`/`I`-or-`i` rows they aggregate | postprocessing    |
| `Harmony_Numerals_.+_(Count\|Per)` | No. / proportion of harmonic annotations for each numeral, as included in the "Numeral" column of the harmonic analysis                                                                                                                   | `harmony`         |
| `Harmony_Additions_.+`             | Proportion of annotations whose "changes" field falls in each bucket: `4_6_64` (suspension/six-four figures), `+9`, `Others_+` (other added notes), `Others_no_+` (other alterations). Only non-empty buckets are emitted; the denominator is the total number of annotation rows | `harmony`         |
| `Part.+_Spe_avg_abs_step_.+`       | 1. split the part into melodic segments (a new segment starts after every rest, and each notated voice is its own sequence of segments); 2. resample each segment on a regular grid of *S* quarter lengths (each note covers its span of grid points); 3. take the discrete finite difference of the resampled MIDI pitches inside each segment — differences never cross a rest — and divide by step *S*; 4. this feature is the mean of the absolute differences pooled over all segments. Step sizes *S* are defined in `musif.extract.features.melody.constants.MOTION_STEPS`. NaN when no segment has enough samples. `Spe` and `Acc` do not depend on the smoothing window, so they carry no `_win_` suffix. Part scope only | `melody`          |
| `Part.+_Acc_avg_abs_step_.+`       | Same as `Spe_avg_abs` but uses the second-order discrete finite difference (i.e. second-order discrete derivative) within each segment. Part scope only                                                                                    | `melody`          |
| `Part.+_Asc_avg_step_.+_win_.+`    | 1. smooth each resampled segment with a centered rolling mean of 2·*W*+1 samples (*W* from `musif.extract.features.melody.constants.MOTION_WINS`); 2. a "chunk" is a maximal run of strictly rising smoothed samples within one segment; this feature is the average number of rising steps per chunk, pooling the chunks of all segments. Part scope only | `melody`          |
| `Part.+_Dsc_avg_step_.+_win_.+`    | Like `Asc_avg` but for descending chunks. Part scope only                                                                                                                                                                                 | `melody`          |
| `Part.+_Asc_prp_step_.+_win_.+`    | Proportion of rising steps over all smoothed steps of all segments (see `Asc_avg`). Part scope only                                                                                                                                       | `melody`          |
| `Part.+_Dsc_prp_step_.+_win_.+`    | Proportion of falling steps over all smoothed steps of all segments. Part scope only                                                                                                                                                      | `melody`          |
| `.+_AverageDuration$`              | Average duration of the note values (quarter note = 1, eighth note = 0.5, etc.), excluding zero-duration grace notes. Produced at part/score scope                                                                                        | `rhythm`          |
| `.+_DottedRhythm$`                 | No. of single-dotted figures (a dotted note shorter than one beat followed on the same beat by a shorter note) / (`SoundingMeasures` × beats of the `TimeSignature`). Produced at part/score scope (score = mean over parts with sounding measures) | `rhythm`          |
| `.+_DoubleDottedRhythm$`           | No. of doubly dotted figures (same rule, two dots) / (`SoundingMeasures` × beats of the `TimeSignature`). Produced at part/score scope                                                                                                    | `rhythm`          |
| `.+_RhythmInt$`                    | Sum of the duration of the notes / (`SoundingMeasures` × beats of the `TimeSignature`). Produced at part/score scope                                                                                                                      | `rhythm`          |
| `.+Degree(b+\|#x?\|x)?\d_Count$`   | No. of notes corresponding to the given scale degree in relation to the **main key** of the piece (natural minor reference in minor keys). Produced at part/score scope; the score is the sum over all parts                              | `scale`           |
| `.+Degree(b+\|#x?\|x)?\d_Per$`     | Proportion of the row above over the notes counted for the scope                                                                                                                                                                          | `scale`           |
| `.+Degree(b+\|#\|x)?\d_Count_relative` | No. of notes corresponding to the given scale degree in relation to the **local key** of the passage the note is in (requires MuseScore harmonic analysis). Produced at part/score scope; the score is the sum over all parts         | `scale_relative`  |
| `.+Degree(b+\|#\|x)?\d_Per_relative` | Proportion of the row above over the notes counted for the scope                                                                                                                                                                        | `scale_relative`  |
| `.+_Degree_(Asc\|Desc\|Dasc\|Ddesc\|Nat\|Nonat)_(Count\|Per)` | Grouped degrees relative to the main key: raised (`#`), lowered (`b`), doubly raised (`x`), doubly lowered (`bb`), natural, and non-natural degrees. Added by the `DataProcessor` when `grouped_analysis` is on, for every scope prefix that carries degree columns | postprocessing    |
| `.+_Degree_(Asc\|Desc\|Dasc\|Ddesc\|Nat\|Nonat)_(Count\|Per)_relative` | Same grouping for the local-key degrees                                                                                                                                                                   | postprocessing    |
| `.+_DynAbruptness$`                | Sum of absolute values of ((dynamic marking 2 − dynamic marking 1) / no. of beats under marking 1), divided by the total no. of beats. Produced at part/score scope                                                                        | `dynamics`        |
| `.+_DynGrad$`                      | Sum of absolute values of ((dynamic marking 2 − dynamic marking 1) / no. of beats under marking 1), divided by (no. of dynamic markings − 1). Produced at part/score scope                                                                 | `dynamics`        |
| `.+_DynMean$`                      | Mean of the dynamic values of the part's markings. A repeated identical marking counts once, a full-measure rest inserts a level of 0, and `fp`-style markings contribute both levels. NaN for parts without markings. Produced at part/score scope | `dynamics`        |
| `.+_DynMean_weighted$`             | Sum of (dynamic value × no. of sounding beats in which it is in force) / (`SoundingMeasures` × beats of the TimeSig). Produced at part/score scope                                                                                         | `dynamics`        |
| `.+_Ambitus$`                      | Ambitus (highest minus lowest MIDI pitch), in number of semitones. Produced at part/sound/family/score scope                                                                                                                              | `ambitus`         |
| `.+_HighestNoteIndex$`             | MIDI pitch of the highest note. Produced at part/sound/family/score scope                                                                                                                                                                 | `ambitus`         |
| `.+_LowestNoteIndex$`              | MIDI pitch of the lowest note. Produced at part/sound/family/score scope                                                                                                                                                                  | `ambitus`         |
| `.+_HighestNote$`                  | Highest note, as note name with octave. Produced at part/sound/family/score scope                                                                                                                                                         | `ambitus`         |
| `.+_LowestNote$`                   | Lowest note, as note name with octave. Produced at part/sound/family/score scope                                                                                                                                                          | `ambitus`         |
| `.+_IntervallicKurtosis$`          | Kurtosis of the interval sizes (difference between the MIDI pitch of two notes). Melody features are produced at part/sound/score scope (no family)                                                                                       | `melody`        |
| `.+_IntervallicStd$`               | Sample std of the interval sizes (difference between the MIDI pitch of two notes)                                                                                                                                                         | `melody`        |
| `.+_IntervallicMean$`              | Mean of the interval sizes (difference between the MIDI pitch of two notes)                                                                                                                                                               | `melody`        |
| `.+_IntervallicSkewness$`          | Skewness of the interval sizes (difference between the MIDI pitch of two notes)                                                                                                                                                           | `melody`        |
| `.+_IntervallicTrimDiff$`          | `.+_IntervallicMean` − `.+_TrimmedIntervallicMean`                                                                                                                                                                                        | `melody`        |
| `.+_IntervallicTrimRatio$`         | (`.+_IntervallicMean` − `.+_TrimmedIntervallicMean`) / `.+_IntervallicMean` (NaN when the mean is 0)                                                                                                                                      | `melody`        |
| `.+_AbsoluteIntervallicKurtosis$`  | Kurtosis of the absolute interval sizes (in no. of semitones)                                                                                                                                                                             | `melody`        |
| `.+_AbsoluteIntervallicMean$`      | Mean of the absolute interval sizes (in no. of semitones)                                                                                                                                                                                 | `melody`        |
| `.+_AbsoluteIntervallicSkewness$`  | Skewness of the absolute interval sizes (in no. of semitones)                                                                                                                                                                             | `melody`        |
| `.+_AbsoluteIntervallicStd$`       | Sample std of the absolute interval sizes (in no. of semitones)                                                                                                                                                                           | `melody`        |
| `.+_AbsoluteIntervallicTrimDiff$`  | `.+_AbsoluteIntervallicMean` − `.+_TrimmedAbsoluteIntervallicMean`                                                                                                                                                                        | `melody`        |
| `.+_AbsoluteIntervallicTrimRatio$` | (`.+_AbsoluteIntervallicMean` − `.+_TrimmedAbsoluteIntervallicMean`) / `.+_AbsoluteIntervallicMean` (NaN when the mean is 0)                                                                                                              | `melody`        |
| `.+_TrimmedAbsoluteIntervallicMean$` | Mean of the absolute interval sizes (in no. of semitones), trimming the most extreme 10% at each tail                                                                                                                                   | `melody`        |
| `.+_TrimmedAbsoluteIntervallicStd$` | Sample std of the absolute interval sizes (in no. of semitones), trimming the most extreme 10% at each tail                                                                                                                              | `melody`        |
| `.+_TrimmedIntervallicMean$`       | Mean of the interval sizes (in no. of semitones, positive and negative), trimming the most extreme 10% at each tail                                                                                                                       | `melody`        |
| `.+_TrimmedIntervallicStd$`        | Sample std of the interval sizes (in no. of semitones, positive and negative), trimming the most extreme 10% at each tail                                                                                                                 | `melody`        |
| `.+_AscendingIntervallicMean$`     | Mean of the sizes of the ascending intervals (in no. of semitones); NaN when there are none                                                                                                                                               | `melody`        |
| `.+_AscendingIntervallicStd$`      | Sample std of the sizes of the ascending intervals (in no. of semitones)                                                                                                                                                                  | `melody`        |
| `.+_DescendingIntervallicMean$`    | Mean of the sizes of the descending intervals (negative, in no. of semitones)                                                                                                                                                             | `melody`        |
| `.+_DescendingIntervallicStd$`     | Sample std of the sizes of the descending intervals (in no. of semitones)                                                                                                                                                                 | `melody`        |
| `.+_AscendingIntervals_Count$`     | No. of ascending intervals                                                                                                                                                                                                                | `melody`        |
| `.+_AscendingIntervals_Per$`       | Proportion of `.+_AscendingIntervals_Count` over all intervals                                                                                                                                                                            | `melody`        |
| `.+_DescendingIntervals_Count$`    | No. of descending intervals                                                                                                                                                                                                               | `melody`        |
| `.+_DescendingIntervals_Per$`      | Proportion of `.+_DescendingIntervals_Count` over all intervals                                                                                                                                                                           | `melody`        |
| `.+_Interval[A-Za-z]+-?\d+_(Count\|Per)` | No. / proportion of each specific interval, named by `music21`'s directed name (descending intervals carry a `-` before the number, e.g. `IntervalM-3`)                                                                             | `melody`        |
| `.+_Intervals.+Asc_(Count\|Per)`   | No. / proportion of ascending intervals of a given type (Perfect, Major, Minor, Augmented, Diminished, double variants, WithinOctave, BeyondOctave)                                                                                       | `melody`        |
| `.+_Intervals.+Desc_(Count\|Per)`  | No. / proportion of descending intervals of a given type                                                                                                                                                                                  | `melody`        |
| `.+_Intervals.+All_(Count\|Per)`   | No. / proportion of intervals of a given type in either direction (unisons included). Note: `WithinOctaveAll_Per + BeyondOctaveAll_Per = 1`, and the `Asc` (resp. `Desc`) within/beyond pair sums to `AscendingIntervals_Per` (resp. descending)  | `melody`        |
| `.+_StepwiseMotion.+_(Count\|Per)` | No. / proportion of [Asc, Desc, All] stepwise intervals (seconds, and chromatic unisons such as A1/d1)                                                                                                                                    | `melody`        |
| `.+_Leaps.+_(Count\|Per)`          | No. / proportion of [Asc, Desc, All] leaps (interval of a 3rd or larger)                                                                                                                                                                  | `melody`        |
| `.+_RepeatedNotes_Count$`          | No. of consecutively repeated notes (the pitch does not change: 0 semitones). Equals `IntervalP1_Count` except for enharmonic 0-semitone steps such as `d2`, which `music21` names as seconds                                              | `melody`        |
| `.+_RepeatedNotes_Per$`            | Proportion of `.+_RepeatedNotes_Count` over all intervals                                                                                                                                                                                 | `melody`        |
| `.+AscendingSemitones_Sum$`        | Sum of all the ascending intervals, counted as semitones                                                                                                                                                                                  | `melody`        |
| `.+DescendingSemitones_Sum$`       | Sum of all the descending intervals, counted as semitones (negative)                                                                                                                                                                      | `melody`        |
| `.+_LargestAbsoluteSemitonesAll$`  | Largest interval in number of semitones, disregarding the direction (the interval of largest magnitude in either direction). The `Asc`/`Desc` variants were removed: they duplicated `LargestSemitonesAsc`/`Desc` up to sign               | `melody`        |
| `.+_LargestSemitones(Asc\|Desc\|All)$` | Largest [asc, desc, all] interval in number of semitones, keeping the sign                                                                                                                                                            | `melody`        |
| `.+_LargestInterval(Asc\|Desc\|All)$` | Largest [asc, desc, all] interval, as the notated `music21` interval name                                                                                                                                                              | `melody`        |
| `.+_SmallestSemitones(Asc\|Desc)$` | Smallest [asc, desc] interval in number of semitones, keeping the sign. The `All` and `AbsoluteSemitones` variants were removed: the smallest-magnitude interval degenerates to the unison whenever any note is repeated                    | `melody`        |
| `.+_SmallestInterval(Asc\|Desc)$`  | Smallest [asc, desc] interval, as the notated `music21` interval name                                                                                                                                                                     | `melody`        |
| `.+_MeanInterval$`                 | Interval name of the rounded mean **absolute** interval (it disregards direction, so it is never a descending interval)                                                                                                                   | `melody`        |
| `Score_VoiceReg`                   | For each vocal part, the mean of (MIDI pitch of the note − MIDI pitch of the part's own last note), averaged over the vocal parts                                                                                                          | `lyrics`          |
| `.+_Syllables$`                    | No. of syllables in a given vocal part, as introduced by the music engraver (a note carrying several verses contributes one syllable per verse). Produced at part/score scope                                                              | `lyrics`          |
| `.+_SyllabicRatio$`                | No. of notes / no. of syllables. The score scope is the ratio of the summed notes and syllables of all vocal parts (a note-weighted aggregate). NaN without lyrics                                                                        | `lyrics`          |
| `.+_VoicePresence$`                | `SoundingMeasures` / measures of the part, for each singer in the score                                                                                                                                                                   | `lyrics`          |
| `Presence_of_.+`                   | Inclusion (1/0) of a given part on the score                                                                                                                                                                                              | added if `separate_instrumentation_column` is `true` in the `DataProcessor` configuration (default: `false`)            |
| `Part.+\|Part.+_Texture`           | No. of notes of Part[1] / no. of notes of Part[2], using each part's actual note count. Pairs follow the configured scoring order, so a pair always yields the same column across a corpus                                                 | `texture`         |

## Notes for typical notated corpora

On the opera-seria corpus used to validate these features (and most notated
corpora of the period), several column families are structurally degenerate;
they are kept for generality but carry no information there:

* `DoubleAugmented`/`DoubleDiminished` interval columns and
  `DoubleDottedRhythm` are all zero.
* `m21_ChordBassMotionFeature_0` is the constant `1.0` sentinel that `music21`
  emits when a score contains no chord-symbol objects (notated scores never
  do); features `_1` through `_11` are zero.
* the processed `Degree_.+_Dasc`/`_Ddesc` columns are zero, so
  `Nonat = Asc + Desc` there.
* rare chromatic degrees and interval qualities are all-zero on most corpora;
  wherever a directional count is all-zero, its `All` twin equals the other
  direction.
* `Presence_of_X` is constant 1 for instruments present corpus-wide (voices
  are excluded from `Instrumentation` by design), and equals
  "`Sound<X>_NumberOfParts` is not NaN" row by row.
